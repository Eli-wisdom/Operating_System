Round-robin scheduling allows each ready task to run in a cyclic queue for a fixed period of time. This algorithm also allows for process execution without starvation.

INPUT

PROCESS 
OUTPUT



file docs

ab
contain everything from the root location to that particular file.with recent 


relative
we do not have the full list .it only specify the name and the fle manager will comput the full length.